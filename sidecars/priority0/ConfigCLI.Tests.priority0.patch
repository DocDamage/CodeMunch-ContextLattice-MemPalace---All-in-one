Target: `tests/ConfigCLI.Tests.ps1` (new test file if absent)

Reason:
- lock in the Priority 0 shift away from `Write-Host` for reusable-module status output
- verify command-data output stays on the pipeline while status messaging moves to information/warning/error streams

---

## Patch A: validate `--get` style output remains pipeline data

```powershell
Describe "ConfigCLI Priority 0 regressions" {
    BeforeAll {
        . "$PSScriptRoot/../module/LLMWorkflow/core/ConfigCLI.ps1"
    }

    It "Returns queried values on the output stream instead of host-only output" {
        # Adjust the command invocation below to the module's actual public getter path.
        # The key assertion is that returned config values remain capturable.

        $result = $null

        # Example placeholder shape:
        # $result = Invoke-ConfigCLI -Get 'execution.mode'

        $result | Should -Not -BeNullOrEmpty
    }
}
```

## Patch B: validate status messages are no longer host-locked

```powershell
It "Uses information output for successful config set operations" {
    # Intended shape:
    # 1. invoke the config set path
    # 2. capture stream 6 (information stream)
    # 3. assert success message appears there

    # Example placeholder:
    # $info = Invoke-ConfigCLI -Set 'execution.mode=fast' 6>&1
    # ($info | Out-String) | Should -Match 'Configuration saved'

    $true | Should -BeTrue
}
```

## Stronger future assertion shape

```powershell
# Future stronger version:
# - verify no `Write-Host` calls remain in the specific execution paths under test
# - assert `Write-Output` is still used for actual values returned by query/get commands
# - assert invalid-mode or invalid-set syntax still goes to `Write-Error`
```

Why this belongs:
- reusable modules should not hide status messages in host-only output
- the Priority 0 change is about making behavior testable and pipeline-safe without changing real data return semantics
